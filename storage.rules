
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /profile_pictures/{userId} {
      // Allow read access by anyone
      allow read: if true;
      // Allow write access only for the authenticated user
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
